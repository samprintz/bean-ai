# bean-ai Specification

## Project Overview

`bean-ai` is a Python application
to classify Beancount transactions with AI.

It provides the methods defined in `cli.py`.

It can be executed as CLI tool `bean-ai`.
To this end, it can be installed e.g. with `pipx`.

It can also be imported from another Python module:
`from bean_ai import predict`.

